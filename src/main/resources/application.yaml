spring:
  application:
    name: hopetail-be

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/hopetail_db?autoReconnect=true
    username: root
    password: 'doitmysql'

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui
    display-request-duration: true

